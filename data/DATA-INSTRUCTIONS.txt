No empirical or external data are required for v1.0.0.

All scientific curves are deterministic evaluations of equations in the frozen
target paper and the documented finite-grid diagnostic.
Machine-readable generated data are written to outputs/.

Future v2.0.0 simulation curves must use the semantic fields in
outputs/epps-overlay-v1.csv and record numerical resolution, alignment, domain,
uncertainty, parameter profile, and software version.

The accepted v1.2.5 checkpoint adds no empirical data. The files
legacy-target-inputs-v1.2.npz and legacy-target-inputs-v1.2.json contain the
declared synthetic clock and realised-draw inputs for the two legacy targets.
The stored arrays and hashes are authoritative. Do not regenerate them during
the ordinary reproduction route; scripts/05_generate_legacy_inputs.py is used
only when intentionally creating a new declared ensemble.

The accepted v1.3.0 checkpoint writes deterministic simulation results to
outputs/legacy-target-execution-v1.3.npz with a validated JSON contract beside
it. These stored results are generated outputs, not empirical data. The
ordinary route validates rather than regenerates them; script
scripts/06_generate_legacy_target_execution.py performs an intentional full
execution.

The accepted v1.3.1 checkpoint derives three deterministic files from the frozen execution:
legacy-epps-curve-v1.3.csv, legacy-epps-path-correlations-v1.3.csv, and
legacy-epps-spectrum-v1.3.csv. They distinguish the path statistics from the
historical plotted curve and its code-defined ribbon. These outputs are
regenerated by scripts/07_generate_legacy_epps.py during the ordinary route.

The accepted v1.3.2 checkpoint derives legacy-shock-selected-paths-v1.3.csv and
legacy-shock-selected-summary-v1.3.csv from publication frames 1 and 9 in the
same frozen execution. The first file retains all five paths; the second gives
pointwise mean, sample standard deviation, minimum, and maximum. They are
regenerated by scripts/08_generate_legacy_shock_views.py during the ordinary
route and contain no empirical observations.

The v1.3.3 audit checkpoint adds no empirical data. The generated files
diagnostics/port-audit-register-v1.3.csv,
diagnostics/port-audit-metrics-v1.3.csv, and
outputs/legacy-shock-relaxation-audit-v1.3.csv contain classifications and
deterministic quantities derived from frozen source, configuration, inputs, and
execution arrays. Regenerate them with scripts/09_run_port_audit.py.
The accompanying operational-time/subordination architecture record is a
planning decision only: no target dynamics, clocks, or subordinated paths are
generated at v1.3.3.

The v1.3.4 gate adds no empirical data and no simulated target paths. The file
diagnostics/operational-primitive-checks-v1.3.csv contains ten deterministic
formula, moment, recurrence and initialization checks. Regenerate it with
scripts/10_run_operational_primitive_checks.py.

The v1.3.5 gate adds no empirical data, target paths, clocks, or random seeds.
The target innovation functions consume externally supplied standard arrays.
The file diagnostics/operational-innovation-checks-v1.3.csv contains eight
deterministic scale, dependence, bound, and probability checks. Regenerate it
with scripts/11_run_operational_innovation_checks.py.

The v1.4.0 gate adds no empirical data, target ensemble, clock or complete
path. The file diagnostics/operational-solver-entry-v1.4.csv contains eight
deterministic boundary, fixed-point, uniform-step and term-decomposition
checks. Regenerate it with scripts/12_run_operational_solver_entry_checks.py.

The v1.4.1 path gate still requires no empirical data. Innovations and shock
fields are explicit caller-supplied arrays. The file
diagnostics/operational-path-checks-v1.4.csv contains eight deterministic
rolling-history, burn-in, thickness and timing checks. Regenerate it with
scripts/13_run_operational_path_checks.py.

The v1.4.2 robustness gate adds no empirical data. Its 15 summary checks are
stored in diagnostics/operational-robustness-checks-v1.4.csv and its 21
grid/domain/history/thickness convergence rows are stored in
outputs/operational-robustness-series-v1.4.csv. Regenerate both with
scripts/14_run_operational_robustness_checks.py.

The v1.4.3 gate adds no empirical data and no calendar-time observations. The
random seed in config/config-v1.4.json constructs deterministic external
operational innovations in scripts/15_run_operational_ensemble.py; the target
ensemble functions still own no random generator. Generated files contain the
aggregate and path-level operational Epps curves, ensemble summary, paired
shock response, selected densities and 19 checks. The shock is explicitly an
external order-density impulse and is not an observed limit order, market
order or trade. Regenerate the evidence with
scripts/15_run_operational_ensemble.py.

The prepared v1.5.0 gate adds no empirical data. Its two clock paths are small
declared deterministic fixtures in config/config-v1.5.json, not samples
presented as a fitted clock law. The observation functions own no random
generator. diagnostics/clock-subordination-checks-v1.5.csv contains 15
contract checks and outputs/clock-subordination-fixture-v1.5.csv contains 52
machine-readable identity and book-specific evaluations. Regenerate both with
scripts/16_run_clock_subordination_checks.py. Stochastic clock ensembles enter
at v1.5.1; calendar-time estimators remain deferred to v1.5.2.

The prepared v1.5.1 gate adds no empirical observations. The intentional
generation command `python scripts/17_generate_clock_ensembles.py` creates
three declared synthetic scenarios from the seeds in config/config-v1.5.1.json
and stores their complete interval and cumulative clock paths in
outputs/clock-ensembles-v1.5.npz. Its JSON companion records the configuration,
law parameters, stream and seed provenance, array hashes and archive hash.
Stored float64 arrays, not cross-version RNG equivalence, define the ensemble.
The ordinary route validates rather than regenerates the archive through
scripts/18_run_clock_ensemble_checks.py, which writes 20 checks and 96
path-book summary rows. Positive-stable sample means in that summary are
finite-sample descriptions only; the corresponding theoretical increment
mean need not exist for alpha_t below one.

The prepared v1.5.2 gate adds no empirical observations. The ordinary route
now stores the complete deterministic Stage 4 boundary prices in
outputs/operational-boundary-paths-v1.4.npz with a hash-validated JSON
companion. This archive is clock free and lets the observation route change
clocks without rerunning or mutating a boundary path. The calendar estimator
writes outputs/calendar-epps-curves-v1.5.csv,
outputs/calendar-epps-member-correlations-v1.5.csv and
outputs/calendar-subordination-summary-v1.5.csv. These retain every lag's
window and activity counts, all member correlations, the two-clock supports
and the operational-path grouping. Regenerate the bridge through
scripts/15_run_operational_ensemble.py and the calendar outputs through
scripts/19_generate_calendar_epps.py. There is no interpolation, empirical
clock calibration or terminal-state extension.

The accepted v1.6.1 gate adds no empirical observations and does not regenerate
the accepted operational or clock archives. It crosses their stored paths in
an eight-cell computational design. The declared cells are listed in
outputs/mechanism-factorial-design-v1.6.csv. Aggregate and member estimates are
stored in outputs/mechanism-factorial-curves-v1.6.csv and
outputs/mechanism-factorial-members-v1.6.csv. Exact pooled and grouped
difference-in-differences terms are in
outputs/mechanism-decomposition-curves-v1.6.csv. The stability register records
the accepted input identifiers, pairing, support and cleanup boundary. Run
scripts/20_run_mechanism_entry.py only after the Stage 4 path and Stage 5 clock
archives are present and hash valid.

v1.6.2 adds no empirical data and no new stochastic input archive. Its
robustness route reads the accepted v1.6.1 member and decomposition CSV files
under exact SHA-256 control, then writes deterministic leave-one-operational-
path-out summaries. No download, calibration, solver rerun, clock generation
or random seed is required for this closing analysis.

The prepared v1.7.0 gate adds no empirical data and no new simulated paths or
clocks. It assembles new Figure 16 from exact-hash accepted Stage 4 operational
curves, Stage 5 subordinated curves and their member records. The realised
clock archive is read only to verify that the two Poisson book clocks remain
distinct. Figure 7 data and images are hash-protected as the immutable legacy
test/reference case. Regenerate the new outputs with
scripts/22_generate_updated_epps_target.py after the accepted earlier routes.

The accepted v1.7.1 gate adds no empirical data, fitted parameter or new
stochastic path. It reads the exact-hash accepted v1.7.0 curve, Stage 4
operational boundary paths and Stage 5 realised Poisson clocks. The main curve
uses a declared rate-based seconds map and a finite-window normalisation from
accepted lags 501--600. Autocorrelation and Welch spectral diagnostics are
computed from the explicitly subordinated one-step boundary-price increments.
Regenerate the four machine-readable outputs and Figure 17 with
scripts/23_generate_simulated_figure_6.py.

The accepted v1.7.2 gate adds no empirical data, stochastic input, calibration
or solver execution. It reads the exact-hash accepted analytical Figure 6 data
and simulated Figure 17 curve, joins only existing common calendar-time
abscissae and writes the overlay and signed-difference registers. No
interpolation is used. Regenerate the two machine-readable outputs and Figure
18 with scripts/24_generate_analytical_simulation_overlay.py.

The accepted v1.7.3 gate adds no empirical data, stochastic path, clock path or
simulation calibration. config/config-v1.7.3.json freezes the proposed
conformity experiments and outputs/theory-conformity-design-v1.7.csv lists the
six reduced-reference and thick-boundary cells. The 25 deterministic design
checks are written to diagnostics/theory-conformity-design-checks-v1.7.csv by
scripts/25_run_theory_conformity_design.py. The design script imports no
solver, clock generator, subordination operation, legacy implementation or RNG.

The accepted v1.7.4 gate adds synthetic clock-only conformity data, not
empirical market observations. The reduced tier generates 32 correlated-
Brownian paths and independent previous-refresh clocks from the explicit seeds
in config/config-v1.7.4.json. The full tier generates 16 identity-clock
calibration and 32 clocked validation paths with the accepted uniform
reaction-boundary solver. The clock transform owns no RNG and is applied only
after operational paths are complete. Regenerate Figure 19, the 40-row curve
file, two-row summary, boundary-price NPZ and 22 checks with
scripts/26_run_clock_only_conformity.py. The exact equal-rate book rate is
0.1 per second; the 0.2 per-second minimum-wait rate is retained only as a
rejected diagnostic. No empirical calibration or combined-curve fit occurs.

The accepted v1.7.5 gate adds synthetic coupling-only conformity evidence,
not empirical observations. The reduced tier uses externally generated normal
arrays from the explicit seeds in config/config-v1.7.5.json; the response
module owns no random generator. The thick-boundary tier is deterministic and
uses eight signed translation-mode perturbations with paired uncoupled
controls under identity clocks. Regenerate Figure 20, the 20-row reduced curve,
six-row rate scan, 7,728-row local-response file, two-row summary, compressed
response archive and 23 checks with
scripts/27_run_coupling_only_conformity.py. Because no positive local rate is
bracketed, no stochastic thick-boundary Epps data or combined prediction is
generated at this gate.

The accepted v1.7.6 gate is a deterministic design audit and adds no
empirical observations or stochastic paths. Regenerate the four-row candidate
register in outputs/coupling-correction-candidates-v1.7.csv, the nine-row
signed one-step response probe in
outputs/coupling-translation-mode-probe-v1.7.csv, and the 26-check diagnostic
register in diagnostics/coupling-correction-design-checks-v1.7.csv with
scripts/28_run_coupling_correction_design.py. The probe injects the proposed
translation-mode field through the accepted solver's external-source entry;
it does not install the production coupling planned for v1.7.7.

The accepted v1.7.7 gate adds synthetic corrected-coupling recovery data,
not empirical observations. Regenerate public Figure 8, the 20-row validation curve,
11-row deterministic rate register, 1,288-row response file, one-row recovery
summary, compressed 32-path validation archive and 38 checks with
scripts/29_run_corrected_coupling_recovery.py. Sixteen calibration paths freeze
the pair-centre variance-growth normalization on lags disjoint from the 32
holdout paths and 20 validation lags. The recurrence uses the fixed uniform
operational grid and identity clock only; no nonuniform update, subordination,
calendar interpolation or combined-curve fit occurs.

The accepted v1.7.11 corrected gate adds synthetic combined clock and corrected-
coupling evidence, not empirical observations. Regenerate Figure 22, the
20-row combined curve and residual file, three-row summary, four-row realised
clock-rate register, compressed component/path archive and 38 checks with
scripts/30_run_combined_no_refit_prediction.py. The reduced and thick-boundary
books complete on uniform operational grids before two independent equal-rate
previous-refresh clocks select prior completed states. All v1.7.4 and v1.7.7
component rates and the accepted covariance scale are frozen; no interpolation
or component/combined refit occurs. Five threshold misses are retained as
scientific qualifications after the exact reduced estimator-aware gate passes.

The accepted v1.7.12 closure adds no simulation or empirical data. Run
scripts/31_run_stage7_closure.py to regenerate the 28-row closure check register
and one-row closure summary. These outputs audit the accepted Stage 7 evidence,
manifest partition, runtime contract, workspace hygiene, figure inventory and
architectural boundary; they do not alter the accepted scientific outputs.

The provisional v1.8.0 event-semantics entry adds no empirical data, stochastic
path or scientific figure. Run scripts/32_run_event_semantics_entry.py to
regenerate diagnostics/event-semantics-entry-checks-v1.8.csv and
outputs/event-semantics-register-v1.8.csv. The deterministic fixture is stored
in config/config-v1.8.0.json. Its execution prices are consumed-cell proxies,
not market observations, and the legacy Figures 8--9 shock is not relabelled as
a trade-impact measurement.

The v2.1.0 long-memory clock comparison adds synthetic clock and order-flow
inputs, not empirical observations. `config/config-v2.1.0-clock-impact.json`
declares caller-owned
seeds for independent book-specific Mittag--Leffler and exponentially tempered
Mittag--Leffler renewal paths, plus heavy-tailed meta-order sign-run inputs.
Run `scripts/43_run_long_memory_clock_impact.py` to regenerate Figures 13
and 14, the two compressed evidence archives, the impact curve CSV, clock
summary and science/mathematics checks. The operational paths are completed
before clocks are applied. No wait is fitted, clipped or inserted into the
density recurrence, and no market data are required.
